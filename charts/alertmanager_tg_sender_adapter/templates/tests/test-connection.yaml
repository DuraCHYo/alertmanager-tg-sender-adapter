apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alertmanager_tg_sender_adapter.fullname" . }}-test-connection"
  labels:
    {{- include "alertmanager_tg_sender_adapter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alertmanager_tg_sender_adapter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
