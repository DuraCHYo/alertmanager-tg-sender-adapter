apiVersion: v1
kind: Service
metadata:
  name: {{ include "alertmanager_tg_sender_adapter.fullname" . }}
  labels:
    {{- include "alertmanager_tg_sender_adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alertmanager_tg_sender_adapter.selectorLabels" . | nindent 4 }}
